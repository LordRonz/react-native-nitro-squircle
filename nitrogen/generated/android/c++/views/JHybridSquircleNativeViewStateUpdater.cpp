///
/// JHybridSquircleNativeViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridSquircleNativeViewStateUpdater.hpp"
#include "views/HybridSquircleNativeViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitrosquircle::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridSquircleNativeViewState>;

std::shared_ptr<const HybridSquircleNativeViewProps> JHybridSquircleNativeViewStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridSquircleNativeViewState& data = concreteState->getData();
  const std::shared_ptr<const HybridSquircleNativeViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridSquircleNativeViewState's data doesn't contain any props!");
  }
  return props;
}

void JHybridSquircleNativeViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridSquircleNativeViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridSquircleNativeViewSpec> hybridView = javaView->getJHybridSquircleNativeViewSpec();
  std::shared_ptr<const HybridSquircleNativeViewProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridSquircleNativeViewProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->cornerSmoothing.isProvided()
        : !newProps->cornerSmoothing.hasSameValue(oldProps->cornerSmoothing)) {
    hybridView->setCornerSmoothing(newProps->cornerSmoothing.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleBackgroundColor.isProvided()
        : !newProps->squircleBackgroundColor.hasSameValue(oldProps->squircleBackgroundColor)) {
    hybridView->setSquircleBackgroundColor(newProps->squircleBackgroundColor.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleBorderColor.isProvided()
        : !newProps->squircleBorderColor.hasSameValue(oldProps->squircleBorderColor)) {
    hybridView->setSquircleBorderColor(newProps->squircleBorderColor.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleBorderWidth.isProvided()
        : !newProps->squircleBorderWidth.hasSameValue(oldProps->squircleBorderWidth)) {
    hybridView->setSquircleBorderWidth(newProps->squircleBorderWidth.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleBorderStyle.isProvided()
        : !newProps->squircleBorderStyle.hasSameValue(oldProps->squircleBorderStyle)) {
    hybridView->setSquircleBorderStyle(newProps->squircleBorderStyle.get());
  }
  if (oldProps == nullptr
        ? newProps->topLeftRadius.isProvided()
        : !newProps->topLeftRadius.hasSameValue(oldProps->topLeftRadius)) {
    hybridView->setTopLeftRadius(newProps->topLeftRadius.get());
  }
  if (oldProps == nullptr
        ? newProps->topRightRadius.isProvided()
        : !newProps->topRightRadius.hasSameValue(oldProps->topRightRadius)) {
    hybridView->setTopRightRadius(newProps->topRightRadius.get());
  }
  if (oldProps == nullptr
        ? newProps->bottomRightRadius.isProvided()
        : !newProps->bottomRightRadius.hasSameValue(oldProps->bottomRightRadius)) {
    hybridView->setBottomRightRadius(newProps->bottomRightRadius.get());
  }
  if (oldProps == nullptr
        ? newProps->bottomLeftRadius.isProvided()
        : !newProps->bottomLeftRadius.hasSameValue(oldProps->bottomLeftRadius)) {
    hybridView->setBottomLeftRadius(newProps->bottomLeftRadius.get());
  }
  if (oldProps == nullptr
        ? newProps->overflowHidden.isProvided()
        : !newProps->overflowHidden.hasSameValue(oldProps->overflowHidden)) {
    hybridView->setOverflowHidden(newProps->overflowHidden.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleShadowColor.isProvided()
        : !newProps->squircleShadowColor.hasSameValue(oldProps->squircleShadowColor)) {
    hybridView->setSquircleShadowColor(newProps->squircleShadowColor.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleShadowOpacity.isProvided()
        : !newProps->squircleShadowOpacity.hasSameValue(oldProps->squircleShadowOpacity)) {
    hybridView->setSquircleShadowOpacity(newProps->squircleShadowOpacity.get());
  }
  if (oldProps == nullptr
        ? newProps->squircleShadowRadius.isProvided()
        : !newProps->squircleShadowRadius.hasSameValue(oldProps->squircleShadowRadius)) {
    hybridView->setSquircleShadowRadius(newProps->squircleShadowRadius.get());
  }
  if (oldProps == nullptr
        ? newProps->shadowOffsetX.isProvided()
        : !newProps->shadowOffsetX.hasSameValue(oldProps->shadowOffsetX)) {
    hybridView->setShadowOffsetX(newProps->shadowOffsetX.get());
  }
  if (oldProps == nullptr
        ? newProps->shadowOffsetY.isProvided()
        : !newProps->shadowOffsetY.hasSameValue(oldProps->shadowOffsetY)) {
    hybridView->setShadowOffsetY(newProps->shadowOffsetY.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::nitrosquircle::views
