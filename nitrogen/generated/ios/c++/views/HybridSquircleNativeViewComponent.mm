///
/// HybridSquircleNativeViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridSquircleNativeViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSquircleNativeViewSpecSwift.hpp"
#import "NitroSquircle-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitrosquircle;
using namespace margelo::nitro::nitrosquircle::views;

/**
 * Represents the React Native View holder for the Nitro "SquircleNativeView" HybridView.
 */
@interface HybridSquircleNativeViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridSquircleNativeViewComponent {
  std::shared_ptr<HybridSquircleNativeViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSquircleNativeViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSquircleNativeViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridSquircleNativeViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridSquircleNativeViewSpec> hybridView = NitroSquircle::NitroSquircleAutolinking::createSquircleNativeView();
    _hybridView = std::dynamic_pointer_cast<HybridSquircleNativeViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroSquircle::HybridSquircleNativeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroSquircle::HybridSquircleNativeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridSquircleNativeViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridSquircleNativeViewProps*>(oldProps.get());
  NitroSquircle::HybridSquircleNativeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // cornerSmoothing: number
    if (oldViewProps == nullptr
          ? newViewProps.cornerSmoothing.isProvided()
          : !newViewProps.cornerSmoothing.hasSameValue(oldViewProps->cornerSmoothing)) {
      swiftPart.setCornerSmoothing(newViewProps.cornerSmoothing.get());
    }
    // squircleBackgroundColor: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleBackgroundColor.isProvided()
          : !newViewProps.squircleBackgroundColor.hasSameValue(oldViewProps->squircleBackgroundColor)) {
      swiftPart.setSquircleBackgroundColor(newViewProps.squircleBackgroundColor.get());
    }
    // squircleBorderColor: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleBorderColor.isProvided()
          : !newViewProps.squircleBorderColor.hasSameValue(oldViewProps->squircleBorderColor)) {
      swiftPart.setSquircleBorderColor(newViewProps.squircleBorderColor.get());
    }
    // squircleBorderWidth: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleBorderWidth.isProvided()
          : !newViewProps.squircleBorderWidth.hasSameValue(oldViewProps->squircleBorderWidth)) {
      swiftPart.setSquircleBorderWidth(newViewProps.squircleBorderWidth.get());
    }
    // squircleBorderStyle: enum
    if (oldViewProps == nullptr
          ? newViewProps.squircleBorderStyle.isProvided()
          : !newViewProps.squircleBorderStyle.hasSameValue(oldViewProps->squircleBorderStyle)) {
      swiftPart.setSquircleBorderStyle(static_cast<int>(newViewProps.squircleBorderStyle.get()));
    }
    // topLeftRadius: number
    if (oldViewProps == nullptr
          ? newViewProps.topLeftRadius.isProvided()
          : !newViewProps.topLeftRadius.hasSameValue(oldViewProps->topLeftRadius)) {
      swiftPart.setTopLeftRadius(newViewProps.topLeftRadius.get());
    }
    // topRightRadius: number
    if (oldViewProps == nullptr
          ? newViewProps.topRightRadius.isProvided()
          : !newViewProps.topRightRadius.hasSameValue(oldViewProps->topRightRadius)) {
      swiftPart.setTopRightRadius(newViewProps.topRightRadius.get());
    }
    // bottomRightRadius: number
    if (oldViewProps == nullptr
          ? newViewProps.bottomRightRadius.isProvided()
          : !newViewProps.bottomRightRadius.hasSameValue(oldViewProps->bottomRightRadius)) {
      swiftPart.setBottomRightRadius(newViewProps.bottomRightRadius.get());
    }
    // bottomLeftRadius: number
    if (oldViewProps == nullptr
          ? newViewProps.bottomLeftRadius.isProvided()
          : !newViewProps.bottomLeftRadius.hasSameValue(oldViewProps->bottomLeftRadius)) {
      swiftPart.setBottomLeftRadius(newViewProps.bottomLeftRadius.get());
    }
    // overflowHidden: boolean
    if (oldViewProps == nullptr
          ? newViewProps.overflowHidden.isProvided()
          : !newViewProps.overflowHidden.hasSameValue(oldViewProps->overflowHidden)) {
      swiftPart.setOverflowHidden(newViewProps.overflowHidden.get());
    }
    // squircleShadowColor: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleShadowColor.isProvided()
          : !newViewProps.squircleShadowColor.hasSameValue(oldViewProps->squircleShadowColor)) {
      swiftPart.setSquircleShadowColor(newViewProps.squircleShadowColor.get());
    }
    // squircleShadowOpacity: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleShadowOpacity.isProvided()
          : !newViewProps.squircleShadowOpacity.hasSameValue(oldViewProps->squircleShadowOpacity)) {
      swiftPart.setSquircleShadowOpacity(newViewProps.squircleShadowOpacity.get());
    }
    // squircleShadowRadius: number
    if (oldViewProps == nullptr
          ? newViewProps.squircleShadowRadius.isProvided()
          : !newViewProps.squircleShadowRadius.hasSameValue(oldViewProps->squircleShadowRadius)) {
      swiftPart.setSquircleShadowRadius(newViewProps.squircleShadowRadius.get());
    }
    // shadowOffsetX: number
    if (oldViewProps == nullptr
          ? newViewProps.shadowOffsetX.isProvided()
          : !newViewProps.shadowOffsetX.hasSameValue(oldViewProps->shadowOffsetX)) {
      swiftPart.setShadowOffsetX(newViewProps.shadowOffsetX.get());
    }
    // shadowOffsetY: number
    if (oldViewProps == nullptr
          ? newViewProps.shadowOffsetY.isProvided()
          : !newViewProps.shadowOffsetY.hasSameValue(oldViewProps->shadowOffsetY)) {
      swiftPart.setShadowOffsetY(newViewProps.shadowOffsetY.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroSquircle::NitroSquircleAutolinking::isSquircleNativeViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroSquircle::HybridSquircleNativeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
